# Lead Status Viewer

Minimal local UI for [directives/UI_LEAD_STATUS_VIEW.md](../directives/UI_LEAD_STATUS_VIEW.md).
Reads from `tmp/app.db`. No secrets or environment variables required.

## Install

```bash
pip install streamlit
```

## Run

From the **repository root**:

```bash
streamlit run ui/app.py
```
