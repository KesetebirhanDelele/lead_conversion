# -----------------------------------------------------------------------
# Lead Conversion System — Python dependencies
# Python 3.12+ required
#
# Install all:       pip install -r requirements.txt
# Backend only:      pip install fastapi uvicorn pydantic python-dotenv
# Tests only:        pip install httpx pytest
# UI only:           pip install streamlit openai
# -----------------------------------------------------------------------

# --- Backend (FastAPI server) ---
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# --- Testing ---
httpx>=0.27.0          # required by FastAPI TestClient
pytest>=8.0.0          # optional — unittest tests also run via pytest

# --- UI (Streamlit portals) ---
streamlit>=1.34.0
openai>=1.25.0         # used in ui/student_portal/ai_tutor.py
